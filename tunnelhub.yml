service:
  type: automation
  uuid: ${automationUuid}

package:
  artifact: dist/artifact.zip

configuration:
  runtimeEngine: LAMBDA
  entrypoint: index.handler
  runtime: nodejs22.x
  memorySize: 512
  timeout: 30